# sql-notes

This folder contains **Oracle-oriented SQL examples** that reflect typical enterprise banking patterns
(pagination, latest record per key, snapshot logic).

These are notes/examples only (the demo app itself does not require a database).
